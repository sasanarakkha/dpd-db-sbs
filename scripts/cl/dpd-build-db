#!/bin/bash

# This script is a wrapper for run_and_log.sh
# It executes scripts/bash/rebuild_db.sh with logging and rotation.

# Determine the directory of this script
PROJECT_ROOT="$HOME/Documents/dpd-db"

MASTER_SCRIPT="$HOME/.local/bin/run_and_log.sh"

LOG_BASE_NAME="rebuild_db"
TARGET_SCRIPT_PATH="${PROJECT_ROOT}/scripts/bash/rebuild_db.sh"

# The command to run, including 'uv run bash'
COMMAND_TO_EXECUTE=(bash -c "cd '${PROJECT_ROOT}' && caffeinate -ismu uv run bash '${TARGET_SCRIPT_PATH}'")

# Execute the master script
bash "${MASTER_SCRIPT}" "${LOG_BASE_NAME}" "${COMMAND_TO_EXECUTE[@]}"

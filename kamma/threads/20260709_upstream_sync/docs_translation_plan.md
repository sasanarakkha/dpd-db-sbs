# Docs Translation Plan — Upstream Sync 2026-07-09

Sync range: `518672a65fa3ea7c36c4c754dc5276bb41f92da7` → `be49bffe2c2c85971784337d4e09915ad1f42800`
Source: `docs_parity_report.md` (generated by `check_docs_parity.py <thread_dir>`).

## Summary of findings

- **Missing translations:** none.
- **Stale translations:** 3 files, all with the exact same one-line diff (upstream switched the
  release-archive format from `.tar.bz2` to `.tar.xz`).
- **Pre-existing condition (not caused by this sync):** all 3 stale files —
  `docs_rus/technical/quick_start.md`, `docs_rus/technical/local_server_setup.md`,
  `docs_rus/technical/use_db.md` — are **not actually translated**. They are near-verbatim copies of
  the English `docs/` source (confirmed via `git log --follow`: unchanged since their original sync
  creation commit `c0c4db6d0`). `use_db.md` has one existing localization (fork release URL
  `sasanarakkha/dpd-db-sbs` instead of `digitalpalidictionary/dpd-db`), otherwise all three are English
  prose.
- **Out of scope, informational only:** `docs_rus/technical/generate_pli2ru_json.md` is flagged
  "unexpected local-only" by the parity script (not in its 3-entry `EXPECTED_LOCAL` allowlist). This is
  a pre-existing local doc unrelated to this sync's diff range — NOTICED, NOT TOUCHING. Registering it
  in the script's allowlist is a separate housekeeping task, not part of this docs-track item.

## Terminology glossary (EN → RU, drawn from existing `docs_rus/` translations)

| English | Russian | Source |
|---|---|---|
| database | база данных | `build_db.md` |
| repository | репозиторий | `build_db.md` |
| submodules | подмодули | `build_db.md` |
| dependencies | зависимости | `build_db.md` |
| release page | страница релизов | `index.md` |
| headword / lemma | словарная форма | `dpd_headwords_table_ru.md` |
| root | корень | `dpd_headwords_table_ru.md`, `rootdict.md` |
| root family | семья/семейство корня | `dpd_headwords_table_ru.md`, `rootdict.md` |
| family word | словарное семейство | `dpd_headwords_table_ru.md` |
| root matrix | матрица корней | `rootdict.md` |
| root dictionary | словарь корней | `rootdict.md` |

## Translation rules

- Keep Pāḷi terms as-is (no transliteration/translation).
- Keep image paths, code blocks, shell commands, and URLs unchanged — **except** where a URL is
  already intentionally localized to the fork's own repo/release page (e.g. `use_db.md`'s existing
  `sasanarakkha/dpd-db-sbs` release link) — preserve that existing localization, don't revert it to
  upstream's URL.
- Translate heading text and alt text.
- Keep HTML anchor IDs and Markdown code-fence language tags unchanged.

## Per-file tasks

All 3 are **stale, targeted update only** — patch exactly the diff evidence below, nothing else. Do
**not** perform a full translation of these files in this pass; they remain English placeholders
except for this one-line fix, matching their current (pre-existing, untranslated) state. Full
translation of these 3 files is a separate, larger task — flag to the user as a candidate for its own
thread, do not expand scope here.

1. **`docs_rus/technical/local_server_setup.md`** (line 40)
   - `wget -qO- https://github.com/digitalpalidictionary/dpd-db/releases/latest/download/dpd.db.tar.bz2 | tar -xj`
     → `wget -qO- https://github.com/digitalpalidictionary/dpd-db/releases/latest/download/dpd.db.tar.xz | tar -xJ`

2. **`docs_rus/technical/quick_start.md`** (line 14)
   - `` Download the latest `dpd.db.tar.bz2` from the [dpd-db releases page](...){target="_blank"} and place it in the root folder. ``
     → `` Download the latest `dpd.db.tar.xz` from the [dpd-db releases page](...){target="_blank"} and place it in the root folder. ``

3. **`docs_rus/technical/use_db.md`** (line 7)
   - `Download **dpd.db.tar.bz2** from [the dpd-db release page](https://github.com/sasanarakkha/dpd-db-sbs/releases/latest/){target="_blank"},`
     → `Download **dpd.db.tar.xz** from [the dpd-db release page](https://github.com/sasanarakkha/dpd-db-sbs/releases/latest/){target="_blank"},`
     (fork release URL preserved as-is, per existing localization — only the filename extension changes)

## Verification (FAST, after execution)

- `uv run python3 kamma/upstream_sync/scripts/check_docs_parity.py kamma/threads/20260709_upstream_sync --strict`
- Prepare commit message only: `#docs: translate/update docs_rus/ for sync 518672a6..be49bffe`

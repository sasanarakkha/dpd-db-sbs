# Plan: Implement Sync Ledger / Manifest Workflow

> **Executing model**: Follow sequentially. Do NOT skip steps. Mark tasks `[~]` before starting,
> `[x]` on completion. Do not make design choices. If ambiguity appears that is not
> resolved by this plan or the locked thread context, raise a `[DISCUSS]` flag and stop.
>
> **Session type**: General sync framework improvements - applies to ALL future sync threads
> **Date**: 2026-04-08

---

## Phase 1: Research and Analysis

> **Status**: Already done in earlier analysis session

- [x] Analyzed the entire `kamma/upstream_sync/` process
- [x] Identified gaps between docs, registry, and scripts
- [x] Defined the sync ledger approach (Option 2: SHA + metadata)
- [x] Kept the 3-stage workflow

---

## Phase 2: Implementation

### Stage 1: Setup Sync State

- [x] Create `kamma/upstream_sync/accepted_sync.json`
- [x] Bootstrap with last accepted upstream SHA from git history
- [x] Add loader helper in `registry_helper.py`

### Stage 2: Refactor Prep

- [x] Modify `prep_analyzer.py` to read from `accepted_sync.json`
- [x] Generate both `prep_report.md` AND `prep_manifest.json`
- [x] Use explicit upstream range (`from_sha..to_sha`)
- [x] Add manifest validation (`load_prep_manifest()`)

### Stage 3: Build Execution Helpers

- [x] Create `finalize_accepted_sync.py` for Stage 3
- [x] Create `sync_runtime.py` for shell automation
- [x] Refactor `full_sync.sh` to use sync state (no inline python)

### Stage 4: Enforcement

- [x] Make `dps_copies` first-class everywhere
- [x] Make parity failures fail (not print)
- [x] Add comprehensive tests

### Stage 5: Documentation

- [x] Update `kamma/upstream_sync/` docs only
- [x] Add explicit note that `docs/` is upstream-only

---

## Phase 3: Verification

- [x] Run validator tests
- [x] Run manifest generation dry-run
- [x] Run finalization dry-run
- [x] Verify shell script syntax

---

## Phase 4: Handoff

- [x] Document changes in `handoff_sync_framework.md`
- [x] Stage all changes

---

## What Was Done (Summary)

1. **New sync state file**: `accepted_sync.json` with last accepted upstream SHA
2. **New Stage 1 output**: Both `prep_report.md` AND `prep_manifest.json`
3. **New Stage 3 finalizer**: `finalize_accepted_sync.py`
4. **New shell automation**: `sync_runtime.py` subcommands
5. **Refactored automation**: `full_sync.sh` reads from sync state
6. **Enforcement**: `dps_copies` everywhere, parity fails
7. **Docs**: Only `kamma/upstream_sync/` touched

---

## Current Status

All changes are **staged** and ready to commit.

Run this to see all changes:
```bash
git diff --cached
```

Commit with:
```bash
git commit -m "#sync prep: add sync state finalizer and execution hooks"
```

---

## Thread Artifacts (Not Staged)

These were generated by dry-run tests - delete if you want:
```bash
rm kamma/threads/implement_sync_process/prep_report.md
rm kamma/threads/implement_sync_process/prep_manifest.json
```